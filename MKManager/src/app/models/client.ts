export class Client {
  constructor(
    public index: string,
    public name: string,
    public target: string,
    public comment: string,
    public host: string,
    public blocked: string
  ) {}
}
